{
  "name": "@mkholt/pdf-thumbnail",
  "version": "1.0.2",
  "description": "React wrapper around PDF-js allowing creation of thumbnails both server and client side",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "github:mkholt/pdf-thumbnail"
  },
  "files": [
    "dist/*"
  ],
  "author": "Morten Holt <mho@delegate.dk>",
  "license": "MIT",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "prepack": "yarn build",
    "postversion": "echo \"Would publish version $npm_package_version\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/react": "^18.3.11",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  },
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "dependencies": {
    "@mkholt/utilities": "^1.0.2",
    "canvas": "2.x",
    "pdfjs-dist": "4.x"
  }
}
